export { normalizeToolOutput } from "./core/normalize";
export { createPlugin } from "./plugin";
export { DEFAULT_CONFIG, resolveConfig } from "./config/defaults";
export type { DetectedType, NormalizedResult, NormalizerConfig } from "./types";
